操作步骤如下

访问 https://zo-computer.cello.so/MRdCccd62ff 完成注册。
注册后先绑0刀卡(不保证一直可用的测试卡:5154620020782510|12|2026|628)。

再使用兑换码 SHEK100 兑换 100 美元额度。
2. 创建 Access Token

在 Zo Computer 的 设置 -> 高级 -> Access Tokens 中创建一个 token，zo_sk_开头的。
3. 反代部署

【文件】- 创建/home/workspace/ai-proxy目录，把server.js（脚本见博客文章中有源码）丢进去。

【托管】- 创建一个”服务”(注意不是”网站”喔)：

服务访问权限：Public

Label: gateway

LocalPort: 8000

Entrypoint: node /home/workspace/ai-proxy/server.js

Working Directory: /home/workspace/ai-proxy

ENV里面：

ZO_ACCESS_TOKEN: 刚才的accesstoken，zo_sk_开头的那个。

PROXY_API_KEY：自己设定的网关外部sk，我用的sk-proxy-gateway-v1。

PROXY_PROMPT_OVERRIDE：true

PROXY_OUTPUT_SANITIZE：false


然后发布服务，通过OpenAI或Anthropic格式来访问该服务域名或自定义域名(如果绑定了)。

/v1/models拉取模型列表。

如下这是一个 ZoComputer API 反向代理服务例子，将 Zo Computer 的 API 转换成 OpenAI/Anthropic 兼容格式。

启动方式：

cd /Users/m/Downloads/shell/work/zo2api

必填：Zo 的 access token
export ZO_ACCESS_TOKEN=”zo_sk_你的token”

可选：自定义外部 API key（不设则随机生成）
export PROXY_API_KEY=”sk-proxy-gateway-v1”

可选：开启越狱提示覆盖
export PROXY_PROMPT_OVERRIDE=true

可选：开启输出清洗（去除 Zo 平台标识）
export PROXY_OUTPUT_SANITIZE=false

启动（默认端口 8000）
node server.js

## Docker 一键部署

先复制环境变量模板：

```bash
cp .env.example .env
```

编辑 `.env`，至少填写：

```bash
ZO_ACCESS_TOKEN=zo_sk_你的token
PROXY_API_KEY=sk-proxy-gateway-v1
PORT=8000
```

一键构建并启动：

```bash
./scripts/deploy.sh
```

查看状态：

```bash
docker compose ps
docker compose logs -f zo2api
```

停止服务：

```bash
docker compose down
```

本地非 Docker 启动：

```bash
./scripts/start.sh
```

健康检查：

```bash
curl http://localhost:8000/healthz
```

访问方式：

OpenAI 格式：http://localhost:8000/v1/chat/completions

Anthropic 格式：http://localhost:8000/v1/messages

模型列表：http://localhost:8000/v1/models

认证：请求头带 Authorization: Bearer sk-proxy-gateway-v1

如果要在 Zeabur 上部署，就是把你消息里描述的那种方式——创建 Service，Entrypoint 设 node /home/workspace/ai-proxy/server.js，环境变量填好就行。
